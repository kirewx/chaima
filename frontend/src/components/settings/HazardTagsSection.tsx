import { SectionHeader } from "./SectionHeader";

export function HazardTagsSection({ groupId: _groupId }: { groupId: string }) {
  return (
    <>
      <SectionHeader title="Hazard tags" />
    </>
  );
}
