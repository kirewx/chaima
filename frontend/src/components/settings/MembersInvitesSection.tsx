import { SectionHeader } from "./SectionHeader";

export function MembersInvitesSection({ groupId: _groupId }: { groupId: string }) {
  return (
    <>
      <SectionHeader title="Members & Invites" />
    </>
  );
}
